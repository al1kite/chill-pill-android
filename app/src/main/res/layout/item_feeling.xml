<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="ddwu.com.mobile.finalreport.feature.dose.DoseViewModel" />
    </data>

    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="10dp">

        <ImageButton
            android:id="@+id/imageButton1"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_margin="2dp"
            android:padding="5dp"
            android:background="@android:color/transparent"
            android:scaleType="fitCenter"
            app:onIconClick="@{viewModel}"
            app:lastClicked="@{viewModel.lastClicked}"
            app:srcCompat="@drawable/icon_awesome"
            app:iconTypeColor="@{viewModel.iconType}" />

        <ImageButton
            android:id="@+id/imageButton2"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_margin="2dp"
            android:padding="5dp"
            android:background="@android:color/transparent"
            android:scaleType="fitCenter"
            app:onIconClick="@{viewModel}"
            app:lastClicked="@{viewModel.lastClicked}"
            app:srcCompat="@drawable/icon_downcast"
            app:iconTypeColor="@{viewModel.iconType}" />

        <ImageButton
            android:id="@+id/imageButton3"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_margin="2dp"
            android:padding="5dp"
            android:background="@android:color/transparent"
            android:scaleType="fitCenter"
            app:onIconClick="@{viewModel}"
            app:lastClicked="@{viewModel.lastClicked}"
            app:srcCompat="@drawable/icon_confounded"
            app:iconTypeColor="@{viewModel.iconType}" />

        <ImageButton
            android:id="@+id/imageButton4"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_margin="2dp"
            android:padding="5dp"
            android:background="@android:color/transparent"
            android:scaleType="fitCenter"
            app:onIconClick="@{viewModel}"
            app:lastClicked="@{viewModel.lastClicked}"
            app:srcCompat="@drawable/icon_mask"
            app:iconTypeColor="@{viewModel.iconType}" />

        <ImageButton
            android:id="@+id/imageButton5"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_margin="2dp"
            android:padding="5dp"
            android:background="@android:color/transparent"
            android:scaleType="fitCenter"
            app:onIconClick="@{viewModel}"
            app:lastClicked="@{viewModel.lastClicked}"
            app:srcCompat="@drawable/icon_explode"
            app:iconTypeColor="@{viewModel.iconType}" />

        <ImageButton
            android:id="@+id/imageButton6"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_margin="2dp"
            android:padding="5dp"
            android:background="@android:color/transparent"
            android:scaleType="fitCenter"
            app:onIconClick="@{viewModel}"
            app:lastClicked="@{viewModel.lastClicked}"
            app:srcCompat="@drawable/icon_angry"
            app:iconTypeColor="@{viewModel.iconType}" />

    </LinearLayout>
</layout>
